/**
 * 
 * Codeforces Round #631 Div.2 (20200403)
 * A. Dreamoon and Ranking Collection
 * */
#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    int t;
    cin >> t;
    while (t--) {
        int n, m;
        int matrix[101];
        cin >> n >> m;
        for (int i = 1; i <= n; i++) {
            cin >> matrix[i];
        }

        for (int i = 1; i <= n; i++) {
            if ()
        }
    }

    return 0;
}