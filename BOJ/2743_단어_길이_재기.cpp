/**
 * 백준 2743번 - 단어 길이 재기
 * 
 * */

#include <iostream>
#include <string>

using namespace std;

int main() {

    cin.tie(NULL);
    ios::sync_with_stdio(false);
    string temp;
    cin >> temp;
    cout << temp.size() << endl;
    return 0;
}